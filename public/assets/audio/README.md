# Audio asset

Place the supplied `struct.mpeg` file in this folder so it is served at:

`/assets/audio/struct.mpeg`

The application already loads this exact URL after the user enters the experience.
